<h1>Disponible books</h1>
<div class="books-container">
    <a href="/">Register a book</a>
    {{#each books}}
        <div>
            #{{this.id}} - <span class="bold"> <a href="/books/{{this.id}}">{{this.title}}</a></span> -
            - {{this.pageqty}} pages
        </div>
    {{/each}}
</div>